#端口号
server:
  port: 8005
spring:
  application:
    name: dispatch
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.221.78:8848
      config:
        server-addr: 192.168.221.78:8848
        file-extension: yaml
    loadbalancer:
      ribbon:
        enabled: false
  config:
    import:
      - optional:nacos:dispatch.yaml
##openFeign整合sentinel熔断
feign:
  sentinel:
    enabled: true

