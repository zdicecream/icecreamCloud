<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdinit.icecreamcloud.authority.sys.mapper.GroupMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zdinit.icecreamcloud.authority.sys.entity.Group">
        <id column="id" property="id" />
        <result column="groupname" property="groupname" />
        <result column="groupcode" property="groupcode" />
        <result column="pid" property="pid" />
        <result column="state" property="state" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, groupname, groupcode, pid, state
    </sql>

</mapper>
